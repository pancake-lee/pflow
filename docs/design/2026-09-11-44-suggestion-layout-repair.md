# #44 行动建议与知识库，术语与布局修复

> 本文档是 [#44](../backlog.md#44-行动建议与知识库并排展示及术语统一) 首轮评估后的修复方案。

## 方案

- 仅在 `docs/archive/` 外统一用户可见中文术语为“行动建议”，保留 `suggest` 命令、包名和 JSON 字段。
- 让并排容器与卡片均使用最小高度、可收缩宽度和内部滚动，移动断点改为单列。
- 以组件源码断言覆盖容器、非固定定位和响应式断点，作为不引入浏览器测试框架时的最小回归检查。
